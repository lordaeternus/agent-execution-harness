# Stetix Adapter

Use shadow mode before replacement mode.

Shadow mode runs the external package in parallel with the local harness.

Replacement mode is allowed only after parity evidence. Keep `.checks/agent-harness-runs` and no product code changes.
