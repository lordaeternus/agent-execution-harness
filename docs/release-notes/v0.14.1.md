# Agent Execution Harness v0.14.1

Patch release focused on weak-agent instruction clarity.

What changed:

- Added a copy-paste execution prompt that names the real harness command instead of a placeholder.
- Clarified that Stetix-style projects should use `pnpm agent:harness`.
- Added test coverage so public docs/templates do not reintroduce missing-command placeholders.

Compatibility:

- No CLI behavior change.
- Existing installs keep working.
- JSON output and automation contracts are unchanged.
