# Agent Execution Harness v0.11.1

Patch release focused on installation clarity.

Fix:

- `init --apply` now returns a clearer success summary for non-technical users.
- The install response explains what happened to `AGENTS.md`: appended, overwritten, created, or left unchanged.
- Next actions now show the exact doctor and rollback commands.

No runtime enforcement behavior changed.
