# Agent Execution Harness v0.12.3

Small CLI usability patch.

Change:

- `agent-harness --version`
- `agent-harness -v`
- `agent-harness version`

now print only the installed package version.

Why:

Users naturally test installs with `--version`. This should be a simple human response, not an unknown-command JSON error.
