# v0.3.0

Token Economy release.

- Added active harness sessions to avoid repeating plan/run flags.
- Added `next` for compact continuation.
- Added `verify` for policy-checked command execution, log capture, `sha256`, `output_ref`, and evidence recording.
- Added `ultra_compact` observations.
- Strengthened token benchmark to require 55% lower agent-facing protocol text.
