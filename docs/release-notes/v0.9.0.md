# Agent Execution Harness v0.9.0

Token-aware harness quality release.

Added:

- control catalog for low-cost auditability
- risk-based sensor profile warnings in `plan-lint`
- `doctor --harnessability` for project readiness scoring
- `doctor --steering` for repeated-failure suggestions
- optional approved fixture validation for critical behavior

The goal is better control without heavier default execution.
