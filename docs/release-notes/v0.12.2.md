# Agent Execution Harness v0.12.2

Small UX polish for first-time installers.

Change:

- `init --apply` now says `Files updated safely` instead of showing technical `merge/create` labels.
- JSON guidance is reduced to a short advanced hint.

Why:

The normal installation path should read like a friendly installer, not a machine response.

No protocol behavior change.
