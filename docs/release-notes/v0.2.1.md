# Agent Execution Harness v0.2.1

Patch release for the token-aware harness line.

Fix:
- token benchmark now measures agent-facing protocol text instead of environment-specific executable paths, making CI stable across operating systems.

No runtime behavior change.
