# v0.6.3

## Added

- `next --exact` returns the exact next command for weak and constrained agents.
- Plan tasks can define `allowed_commands` for deterministic validation.
- CLI ordering failures now emit structured `repair_hint` JSON.

## Validation

- Added weak-agent simulations for exact command execution.
- Added repair classifications for premature claims, missing `file_scope`, wrong evidence type, undeclared files and phase-order mistakes.
