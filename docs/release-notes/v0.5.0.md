# v0.5.0

## Added

- Added governed `learning_memory` for evidence-backed lessons.
- Added `agent-harness learn capture|review|promote|reject|retire|query|prune`.
- Added `agent_harness_lesson_v1` schema.
- Added stale detection for lessons when tracked files change.
- Added secret redaction for lesson summaries and signatures.

## Token Policy

- `learn query --top-k 3` returns compact lessons only.
- Long proof stays in run artifacts; lessons keep evidence references.

## Rollback

- Disable `learning_memory.enabled`.
- Remove the `learn` command usage from project runtime docs.
- Keep existing run artifacts; they remain valid.
