{
  "schema_version": "agent_harness_plan_v1",
  "plan_id": "simulated-bugfix",
  "risk_level": "L2",
  "rollback_expectation": "Remove simulated files.",
  "gates": ["node --version"],
  "tasks": [
    {
      "task_id": "red-green",
      "files": ["simulated.txt"],
      "required_evidence": ["focused_tests", "file_scope"],
      "acceptance_criteria": "Run `node --version` and record focused_tests plus file_scope evidence for the simulated file."
    }
  ]
}
